<%= form_with(model: job_item) do |form| %>
  <% if job_item.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(job_item.errors.count, "error") %> prohibited this job_item from being saved:</h2>

      <ul>
        <% job_item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.textarea :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
