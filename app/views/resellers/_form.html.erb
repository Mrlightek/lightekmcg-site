<%= form_with(model: reseller) do |form| %>
  <% if reseller.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(reseller.errors.count, "error") %> prohibited this reseller from being saved:</h2>

      <ul>
        <% reseller.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
