<%= form_with(model: storyline) do |form| %>
  <% if storyline.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(storyline.errors.count, "error") %> prohibited this storyline from being saved:</h2>

      <ul>
        <% storyline.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
